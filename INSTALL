#dependencies which are not part of the ebuild
emerge

baselayout

openrc shadow util-linux bash coreutils

grep net-tools procps findutils dhcpcd nano

gcc passenger

redmine

#apache need SysV IPC!

#add the apache user
groupadd apache
useradd -g apache apache

redmine config

#uninstall
emerge -C gcc perl python portage (perl-cleaner)

#add the hostname
#nano /etc/hosts

#now apache can be started
#/usr/sbin/apache2 -D DEFAULT_VHOST -D INFO -D SSL -D SSL_DEFAULT_VHOST -D LANGUAGE -D PASSENGER -D FOREGROUND -k start


#uninstall
